export enum QRGradientType {
    LINEAR = 'linear',
    RADIAL = 'radial',
}

export enum QRLevel {
    L = 'L',
    M = 'M',
    Q = 'Q',
    H = 'H',
}

export enum QRRenderAs {
    CANVAS = 'canvas',
    SVG = 'svg',
}
