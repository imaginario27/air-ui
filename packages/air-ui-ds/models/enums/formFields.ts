export enum ControlFieldSize {
    XS = 'xs',
    SM = 'sm',
    MD = 'md',
    LG = 'lg',
}

export enum InputSize {
    MD = 'md',
    LG = 'lg',
}

export enum RadioType {
    DEFAULT = 'default',
    BUTTON = 'button',
}

export enum SwitchStyle {
    BRAND = 'brand',
    SUCCESS = 'success',
}