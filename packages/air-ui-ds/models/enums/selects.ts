export enum SelectSize {
    MD = 'md',
    LG = 'lg',
}

export enum SelectType {
    TEXT = 'text',
    ICON = 'icon',
    USER = 'user',
    IMAGE = 'image',
}

export enum SelectActiveStyle {
    CHECK = 'check',
    FILL = 'fill',
}

export enum CardSelectionMode {
    CARD = 'card',
    BUTTON = 'button',
}